CMAKE_MINIMUM_REQUIRED(VERSION 3.10...4.0)
PROJECT(tests VERSION 0.0.1)

# Adding the test
ADD_EXECUTABLE(tests ./main.c )
TARGET_LINK_LIBRARIES(tests  buffer)
TARGET_INCLUDE_DIRECTORIES(tests PUBLIC ./)
TARGET_INCLUDE_DIRECTORIES(tests PUBLIC ../ctest)
